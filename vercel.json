{
  "version": 2,
  "builds": [
    { "src": "vite.config.ts", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/admin(.*)", "dest": "/" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "cleanUrls": true,
  "trailingSlash": false
}
